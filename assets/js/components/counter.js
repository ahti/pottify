class Counter extends React.Component {
  constructor() {
    this.count = 0
  }

  // handleClick = () => {
  //   this.setState({count: this.count + 1})  
  // }

  // render = () => {
  //   return <h1 onClick={this.handleClick}>{this.count}</h1>
  // }
}
